apiVersion: v1
kind: Service
metadata:
  name: svc-gerenciamento-api
spec:
  type: LoadBalancer
  selector:
    app: gerenciamento-api
  ports:
      - port: 30000
        targetPort: 8080
        protocol: TCP