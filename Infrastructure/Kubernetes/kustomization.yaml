apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - db-secrets.yaml
  - storageclass-ebs-gp3.yaml
  - pvc-gerenciamento-db.yaml
  - deploy-gerenciamento-api.yaml
  - deploy-gerenciamento-db.yaml
  - svc-gerenciamento-api.yaml
  - svc-gerenciamento-db.yaml
  - hpa-gerenciamento-api.yaml

configMapGenerator:
  - name: postgres-init-script
    files:
      - Init.sql=/DbEntrypoint/Init.sql
